import React from 'react'

const Skeleton = ({ className }) => {
    return (

        <div className={`h-60 animate-pulse rounded-md bg-slate-800 w-full ${className}`}>



        </div>
    )
}

export default Skeleton